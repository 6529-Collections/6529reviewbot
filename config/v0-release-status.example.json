{
  "version": 1,
  "release": "v0.1.0",
  "gates": {
    "ledger-schema": {
      "status": "complete",
      "evidence": "Private operator evidence: redacted ledger schema apply log."
    },
    "model-prices": {
      "status": "deferred",
      "notes": "Example only: defer applied price rows when the release keeps conservative default estimates."
    },
    "github-app": {
      "status": "pending",
      "notes": "Example only: create the production 6529bot App before broad release."
    }
  }
}
